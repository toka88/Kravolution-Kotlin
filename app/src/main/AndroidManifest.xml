<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gorantokovic.kravolution">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".BaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.dojo.DojoActivity" />
        <activity android:name=".activities.mybelt.MyBeltActivity" />
        <activity android:name=".activities.scheduler.SchedulerActivity" />
        <activity android:name=".activities.navigation.NavigationActivity" />
        <activity android:name=".activities.auth.ResetPasswordActivity" />
        <activity android:name=".activities.auth.ForgotPasswordActivity" />
        <activity android:name=".activities.auth.BaseAuthActivity" />
        <activity android:name=".activities.auth.RegisterActivity" />
        <activity android:name=".activities.auth.LoginActivity" />
        <activity android:name=".activities.onboarding.OnboardingActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>